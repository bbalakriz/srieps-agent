apiVersion: v1
kind: Secret
metadata:
  name: hermes-rca-bridge-secret
  namespace: hermes-agent
  labels:
    app: hermes-rca-bridge
type: Opaque
stringData:
  HERMES_API_KEY: REPLACE_HERMES_API_KEY
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: hermes-rca-bridge-config
  namespace: hermes-agent
  labels:
    app: hermes-rca-bridge
data:
  HERMES_BASE_URL: REPLACE_HERMES_BASE_URL
  HERMES_MODEL: REPLACE_HERMES_MODEL
  CLUSTER_NAME: REPLACE_CLUSTER_NAME
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes-rca-bridge
  namespace: hermes-agent
  labels:
    app: hermes-rca-bridge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hermes-rca-bridge
  template:
    metadata:
      labels:
        app: hermes-rca-bridge
    spec:
      containers:
        - name: hermes-rca-bridge
          image: quay.io/balki404/sreips-hermes-rca-bridge:0.0.3
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: HERMES_BASE_URL
              valueFrom:
                configMapKeyRef:
                  name: hermes-rca-bridge-config
                  key: HERMES_BASE_URL
            - name: HERMES_MODEL
              valueFrom:
                configMapKeyRef:
                  name: hermes-rca-bridge-config
                  key: HERMES_MODEL
            - name: CLUSTER_NAME
              valueFrom:
                configMapKeyRef:
                  name: hermes-rca-bridge-config
                  key: CLUSTER_NAME
            - name: HERMES_API_KEY
              valueFrom:
                secretKeyRef:
                  name: hermes-rca-bridge-secret
                  key: HERMES_API_KEY
                  optional: true
            - name: HERMES_TIMEOUT_SEC
              value: "600"
            - name: LOG_LEVEL
              value: "INFO"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: hermes-rca-bridge
  namespace: hermes-agent
spec:
  selector:
    app: hermes-rca-bridge
  ports:
    - port: 8000
      targetPort: 8000
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: hermes-rca-bridge
  namespace: hermes-agent
  annotations:
    haproxy.router.openshift.io/timeout: 600s
spec:
  to:
    kind: Service
    name: hermes-rca-bridge
  port:
    targetPort: 8000
  tls:
    termination: edge
---
# bootstrap substitutes REPLACE_* from config.env and captured Hermes route, then oc apply
